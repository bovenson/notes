#include <iostream>
#include <algorithm>

using namespace std;

int main() {
    int n, arr[n][2];
    cin >> n;
    for (int i = 0; i < n; ++i) {
        cin >> arr[i][0] >> arr[i][1];
    }
    sort();
    return 0;
}
