#include <iostream>
#include <vector>

using namespace std;

int main() {
    int i, j, k, n, t, sum, avg, cnt;
    vector<int> v;
    cin >> k;
    for (i = 0; i < k; ++i) {
        cin >> n;
        sum = 0; cnt = 0;
        v.clear();
        for (j = 0; j < n; ++j) {
            cin >> t;
            v.push_back(t);
        }        }
    }
    return 0;
}